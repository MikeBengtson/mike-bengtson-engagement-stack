# Scripts

Scripts are optional conveniences. Engagement Stack does not require a local runtime.

- `render-resumes.sh` renders resume artifacts with local Pandoc or Docker.
- `validate-stack.sh` checks for required files and unresolved TODOs.

GitHub Actions can render resumes without local setup.
